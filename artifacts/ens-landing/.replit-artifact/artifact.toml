kind = "web"
previewPath = "/"
title = "ENS - Exhibition Booth Design Platform"
version = "1.0.0"
id = "artifacts/ens-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18795

[services.development]
run = "pnpm --filter @workspace/ens-landing run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ens-landing", "run", "build" ]
publicDir = "artifacts/ens-landing/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18795"
BASE_PATH = "/"
